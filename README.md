# experiment_3

Practice repo for Git workflow (clone → branch → edit → commit → PR → merge).

## Files
- `hello.py`: prints a greeting.

## How to run
1) Open terminal in this folder
2) Run: `python hello.py`
